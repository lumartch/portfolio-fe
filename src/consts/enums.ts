export enum EPaths {
    About = '/about',
    Contact = '/contact',
    Home = '/',
    Projects = '/projects',
}

export enum EApiPaths {
    BASE_PATH = '/api/v1',
    INFO = '/info',
    REPOS = '/repos',
    USER = '/user'
}

export enum EGitSource {
    GITHUB = 'github', 
    GITLAB = 'gitlab'
}