export enum EPaths {
    Home = "/",
    Projects = "/projects",
    About = "/about",
    Contact = "/contact",
}

export enum EApiPaths {
    BASE_PATH = '/api/user',
    INFO = '/info',
    REPOS = '/repos'
}

export enum EGitSource {
    GITHUB = 'github', 
    GITLAB = 'gitlab'
}