export enum PagePaths {
    ABOUT = 'ABOUT',
    CONTACT = 'CONTACT',
    HOME = 'HOME',
    PROJECTS = 'PROJECTS',
}

export enum ApiPaths {
    BASE_PATH = 'BASE_PATH',
    INFO = 'INFO',
    REPOS = 'REPOS',
    USER = 'USER'
}

export enum GitSource {
    GITHUB = 'github',
    GITLAB = 'gitlab'
}

export enum ColorMode {
    DARK = 'dark',
    LIGHT = 'light'
}

export enum ApiType {
    PROFILE = 'PROFILE',
    REPOS = 'REPOS'
}