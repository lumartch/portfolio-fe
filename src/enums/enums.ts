export enum PagePaths {
    ABOUT = 'ABOUT',
    CONTACT = 'CONTACT',
    HOME = 'HOME',
    PROJECTS = 'PROJECTS',
}

export enum EApiPaths {
    BASE_PATH = '/api/v1',
    INFO = '/info',
    REPOS = '/repos',
    USER = '/user'
}

export enum EGitSource {
    GITHUB = 'github',
    GITLAB = 'gitlab'
}